var showDropMenu = function (name) {

    var dropMenu = document.getElementsByClassName(name)[0];
    dropMenu.style.display = "block";
};

var hideDropMenu = function (name) {
    var dropMenu = document.getElementsByClassName(name)[0];
    dropMenu.style.display = "none";
};